[project]
name = "llm-first-read"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pytest>=8.4.2",
    "pyyaml>=6.0.3",
    "tiktoken>=0.12.0",
    "torch>=2.8.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
